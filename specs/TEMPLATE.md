# Spec NNN — <Feature name>

- Status: Draft | Approved | Done
- Owner:
- Last updated: YYYY-MM-DD
- Depends on: <spec numbers, if any>

## Requirements (what & why)

What this feature must do, in plain language. Number each requirement (R1, R2…)
so tasks and acceptance criteria can reference them. State the "why" briefly.

## Design (how)

The approach: files to create/change, key functions and signatures, data flow,
external tools. Note anything that affects the scene-JSON contract. Call out
trade-offs and rejected alternatives if relevant.

## Tasks (in order)

1. ...
2. ...
Keep each task to a focused, reviewable diff. Mark which tasks touch multiple
files (review gate).

## Acceptance criteria

Concrete, testable checks that prove the feature works (commands to run and
expected results). Map back to the R-numbers.

## Out of scope

What this spec deliberately does not cover.
