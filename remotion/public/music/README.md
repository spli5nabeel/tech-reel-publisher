# Background music tracks

Drop royalty-free `.mp3` files here. They appear in the web UI's
**Background music** dropdown (and via `--music <filename>` on the CLI).

- Only `*.mp3` files are listed.
- Use the bare filename as the value, e.g. `lofi-chill.mp3`.
- The app never downloads or bundles music — supply your own licensed tracks.
